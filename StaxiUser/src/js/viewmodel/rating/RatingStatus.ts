import strings from '../../../res/strings';

enum RatingStatus {
    Low = strings.rating_low,
    Moderate = strings.rating_moderate,
    Good = strings.rating_good,
    Great = strings.rating_great,
    Excellent = strings.rating_excellent
}

export default RatingStatus;